/**
 * Where users report problems. Two places link here — the persistent feedback
 * control and the page-error fallback, which appends `/new` with a prefilled
 * issue — and they must not drift apart if the repo ever moves.
 */
export const ISSUES_URL = 'https://github.com/laulopezreal/KnightMind/issues';
